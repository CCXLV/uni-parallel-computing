[project]
name = "parallel-computing"
version = "0.1.0"
description = "Uni Parallel and Distributed Computing"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.optional-dependencies]
dev = [
    "ruff>=0.15.7",
]
